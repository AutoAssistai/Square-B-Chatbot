# 🔑 دليل إعداد API Key

## المشكلة الحالية

التطبيق يعمل بشكل ممتاز ✅، لكنه يحتاج إلى مفتاح API صالح للتواصل مع الذكاء الاصطناعي.

**الخطأ الذي تراه**:
```
Error code: 401 - No auth credentials found
```

**السبب**: المفتاح في `.env` غير صالح أو منتهي الصلاحية.

---

## ✅ الحل: احصل على API Key مجاني

### الخيار 1: OpenRouter (موصى به - مجاني للتجربة) 🌟

**لماذا OpenRouter؟**
- ✅ رصيد مجاني للتجربة
- ✅ يدعم عدة نماذج (GPT-4, GPT-3.5, Claude, etc.)
- ✅ سهل التسجيل
- ✅ أسعار تنافسية

**خطوات الحصول على المفتاح**:

1. **سجّل في الموقع**:
   - اذهب إلى: https://openrouter.ai
   - اضغط على "Sign Up"
   - سجّل بـ Google أو GitHub أو Email

2. **احصل على API Key**:
   - بعد التسجيل، اذهب إلى: https://openrouter.ai/keys
   - اضغط على "Create Key"
   - انسخ المفتاح (يبدأ بـ `sk-or-v1-...`)

3. **أضف المفتاح في `.env`**:
   ```env
   # في ملف .env، عدّل هذه الأسطر:
   API_KEY=sk-or-v1-your-key-here
   MODEL=openai/gpt-3.5-turbo
   API_BASE_URL=https://openrouter.ai/api/v1
   ```

4. **أعد تشغيل التطبيق**:
   ```bash
   # أوقف التطبيق (Ctrl+C)
   python main.py
   ```

---

### الخيار 2: OpenAI مباشرة

**التكلفة**: يتطلب إضافة بطاقة ائتمانية (لكن الأسعار منخفضة جداً)

**خطوات الحصول على المفتاح**:

1. **سجّل في OpenAI**:
   - اذهب إلى: https://platform.openai.com
   - اضغط على "Sign Up"
   - أكمل التسجيل

2. **أضف طريقة دفع**:
   - اذهب إلى: https://platform.openai.com/account/billing
   - أضف بطاقة ائتمانية

3. **احصل على API Key**:
   - اذهب إلى: https://platform.openai.com/api-keys
   - اضغط على "Create new secret key"
   - انسخ المفتاح (يبدأ بـ `sk-...`)

4. **أضف المفتاح في `.env`**:
   ```env
   # في ملف .env، عدّل هذه الأسطر:
   OPENAI_API_KEY=sk-your-openai-key-here
   OPENAI_MODEL=gpt-4
   
   # وعلّق على (أو احذف) هذه الأسطر:
   # API_KEY=...
   # API_BASE_URL=...
   ```

5. **أعد تشغيل التطبيق**:
   ```bash
   # أوقف التطبيق (Ctrl+C)
   python main.py
   ```

---

## 🧪 اختبار بدون API Key

إذا أردت اختبار التطبيق بدون API Key (لن تعمل الدردشة الذكية لكن ستعمل القائمة):

```bash
python test_chatbot.py
```

هذا يختبر:
- ✅ تحميل القائمة من MENU.txt
- ✅ البحث الذكي (Fuzzy Matching)
- ✅ الفئات والأسعار
- ✅ معلومات التوصيل

---

## 📝 مثال على `.env` صحيح

### باستخدام OpenRouter (موصى به):
```env
# LLM Configuration - OpenRouter
API_KEY=sk-or-v1-abc123def456ghi789
MODEL=openai/gpt-3.5-turbo
API_BASE_URL=https://openrouter.ai/api/v1

# Menu Configuration
MENU_TXT=MENU.txt

# Logging
LOG_FILE=logs/chat.log

# Server
PORT=8000
```

### باستخدام OpenAI مباشرة:
```env
# LLM Configuration - OpenAI
OPENAI_API_KEY=sk-proj-abc123def456
OPENAI_MODEL=gpt-4

# Menu Configuration
MENU_TXT=MENU.txt

# Logging
LOG_FILE=logs/chat.log

# Server
PORT=8000
```

---

## ✅ كيف تعرف أن المفتاح يعمل؟

بعد إضافة المفتاح الصحيح وإعادة التشغيل:

1. **افتح المتصفح**: http://localhost:8000
2. **اكتب رسالة**: "مرحبا"
3. **إذا رد البوت بالعربي** → المفتاح يعمل! ✅
4. **إذا ظهر خطأ** → المفتاح غير صحيح ❌

---

## 🆘 استكشاف الأخطاء

### خطأ: "Invalid API Key"
**الحل**: تأكد من نسخ المفتاح بالكامل بدون مسافات

### خطأ: "Rate limit exceeded"
**الحل**: انتظر قليلاً أو استخدم نموذج أرخص (gpt-3.5-turbo)

### خطأ: "Insufficient credits"
**الحل**: أضف رصيد في حسابك

### التطبيق لا يقرأ `.env` الجديد
**الحل**: أوقف التطبيق تماماً (Ctrl+C) وشغّله مرة أخرى

---

## 💡 نصائح لتوفير التكلفة

1. **استخدم gpt-3.5-turbo بدلاً من gpt-4**:
   - أرخص 10 مرات
   - سريع جداً
   - جودة ممتازة لهذا الاستخدام

2. **استخدم OpenRouter**:
   - رصيد مجاني للتجربة
   - أسعار أقل من OpenAI مباشرة
   - لا يحتاج بطاقة ائتمانية للبدء

3. **قلل max_tokens** (في `chat_service.py`):
   - القيمة الحالية: 500
   - للتوفير: 300
   - الردود ستكون أقصر قليلاً

---

## 📞 هل تحتاج مساعدة؟

إذا واجهت أي مشكلة:

1. تحقق من ملف `logs/chat.log` للأخطاء
2. تأكد من نسخ المفتاح بشكل صحيح
3. جرّب الاختبار البسيط: `python test_chatbot.py`

---

## 🎉 بعد إعداد المفتاح

بمجرد إضافة المفتاح الصحيح، ستحصل على:

✅ دردشة ذكية تعمل بالكامل
✅ ردود بالعربي الأردني الطبيعي
✅ فهم للأخطاء الإملائية
✅ اقتراحات ذكية
✅ جميع الميزات الـ 11 مُفعّلة!

**ابدأ الآن من هنا**: https://openrouter.ai 🚀

# Environment configuration for Square B Arabic Chatbot Demo
PROJECT_NAME="Square B - Arabic Chatbot Demo"

# You can switch between JSON or CSV by changing the path and extension
MENU_FILE="data/menu.json"

# LLM configuration
# Primary (OpenAI-style)
OPENAI_API_KEY="REDACTED_SET_YOUR_KEY"
OPENAI_MODEL="GPT-5"
# Alternative env var names per spec (supported by the app)
API_KEY="REDACTED_SET_YOUR_OPENROUTER_KEY"
MODEL="GPT-5"
# Optional: set a custom base URL for providers like OpenRouter
API_BASE_URL="https://openrouter.ai/api/v1"

# Text menu source (priority)
MENU_TXT="MENU.txt"
# (PDF/JSON/CSV parsing disabled by policy for now)
# MENU_PDF="square_Bmnue.pdf"
# PDF_FILE="square_Bmnue.pdf"

# Caching and logging
MENU_CACHE="data/menu_cache.json"
LOG_FILE="logs/chat.log"

# Optional: change port when running via __main__
PORT=8000

# 🚀 دليل البدء السريع - 5 دقائق

## الخطوات السريعة

### 1️⃣ التثبيت (دقيقة واحدة)

```bash
# استنسخ/حمّل المشروع
cd square-b-chatbot

# ثبّت المتطلبات
pip install -r requirements.txt
```

### 2️⃣ إعداد API Key (دقيقتان)

افتح ملف `.env` وأضف مفتاح API:

```env
# خيار 1: OpenAI
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4

# خيار 2: OpenRouter (مجاني للتجربة)
API_KEY=sk-or-v1-your-key
API_BASE_URL=https://openrouter.ai/api/v1
MODEL=openai/gpt-3.5-turbo
```

**الحصول على مفتاح مجاني**:
- OpenRouter: https://openrouter.ai → أنشئ حساب → احصل على Key

### 3️⃣ التشغيل (30 ثانية)

```bash
python main.py
```

✅ افتح المتصفح: http://localhost:8000

### 4️⃣ التجربة (دقيقة واحدة)

جرب هذه الأسئلة:
- "كم سعر برجر بيف؟"
- "شو بتنصحلي؟"
- "ورجيني المنيو"

---

## اختبار بدون API Key

إذا لم يكن لديك API key، اختبر خدمة القائمة:

```bash
python test_chatbot.py
```

يعرض:
- ✅ تحميل القائمة (36 عنصر)
- ✅ البحث الذكي (fuzzy matching)
- ✅ الفئات والأسعار

---

## المشاكل الشائعة

### خطأ: "Menu file not found"
```bash
# تأكد من وجود MENU.txt
ls -la MENU.txt
```

### خطأ: "Invalid API Key"
```bash
# تحقق من .env
cat .env | grep API
```

### خطأ: "Port 8000 already in use"
```bash
# غيّر المنفذ
PORT=8001 python main.py
```

---

## الخطوات التالية

📚 **توثيق كامل**: اقرأ `README.md`
🚀 **النشر**: اقرأ `DEPLOYMENT.md`
👨‍💻 **التطوير**: اقرأ `DEVELOPER_GUIDE.md`

---

**مبروك! 🎉 تطبيقك يعمل الآن!**
